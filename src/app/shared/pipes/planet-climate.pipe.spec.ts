import { PlanetClimatePipe } from './planet-climate.pipe';

describe('PlanetClimatePipe', () => {
  it('create an instance', () => {
    const pipe = new PlanetClimatePipe();
    expect(pipe).toBeTruthy();
  });
});
