import { PlanetPopulationPipe } from './planet-population.pipe';

describe('PlanetPopulationPipe', () => {
  it('create an instance', () => {
    const pipe = new PlanetPopulationPipe();
    expect(pipe).toBeTruthy();
  });
});
