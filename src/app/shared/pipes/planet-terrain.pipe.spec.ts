import { PlanetTerrainPipe } from './planet-terrain.pipe';

describe('PlanetTerrainPipe', () => {
  it('create an instance', () => {
    const pipe = new PlanetTerrainPipe();
    expect(pipe).toBeTruthy();
  });
});
